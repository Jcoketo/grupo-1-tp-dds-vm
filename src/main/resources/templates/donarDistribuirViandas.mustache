<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Distribuir Viandas</title>
    <!-- Enlace a CSS de Bootstrap -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>

        main {
            flex-grow: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 50px;
            background-color: white;
        }


        .form-control {
            margin-bottom: 30px;
            border: 1px solid #1e5552;
        }

        .heladera-card {
            padding: 15px;
            border: 1px solid #ccc;
            border-radius: 5px;
            margin-bottom: 10px;
        }

        .heladera-card p {
            font-size: 14px;
        }


        .green-box {
            background-color: #1e5552;
            color: white;
            text-align: center;
            padding: 5px;
            border-radius: 5px;
            margin-top: 10px;
            cursor: pointer;
        }
        .green-box:hover {
            background-color: #143c3a;
        }

        .btn {
            border-radius: 10px; /* Bordes redondeados */
            padding: 10px 10px; /* Espaciado interno para que se vean mejor */
            width: 150px; /* Ancho fijo, ajusta según tus necesidades */
            margin-bottom: 10px; /* Espacio entre los botones */
            border: none; /* Eliminar borde predeterminado */
            cursor: pointer; /* Cambiar cursor al pasar sobre el botón */
            font-size: 16px; /* Tamaño de fuente */
        }

        .btn-cancel {
            background-color: #B8CCC8;
            color: black;
        }

        .btn-submit {
            background-color: #1e5552;
            color: white;
        }

        h1, h2, h3, h5, h6 {
            font-size: 19px; /* Ajusta el tamaño de la fuente de los headers */
            font-weight: bold; /* Ajusta el peso de la fuente de los headers */
        }

        h4 {
            font-size: 16px; /* Ajusta el tamaño de la fuente de los headers */
        }

        h3 {
            margin: 0; /* Elimina márgenes por defecto */
            white-space: nowrap; /* Evita que el texto haga salto de línea */
        }

        .error-message {
            color: red;
            font-size: 14px;
            margin-top: 10px;
        }

        .heladeras-container {
            width: 100%;
            margin: 0 auto;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }
        .btn-group {
            display: flex;
            justify-content: center;
            align-items: center;
            margin-top: 20px;
        }

        .btn-cancelar,
        .btn-distribuir {
            border: none;
            padding: 10px 20px; /* Adjusted padding to prevent text overflow */
            vertical-align: middle;
            border-radius: 10px;
            font-size: 16px;
            font-weight: 500;
            margin: 15px;
            width: auto; /* Adjusted width to auto */
            white-space: nowrap; /* Prevent text from wrapping */
            text-align: center; /* Center text inside the button */
        }

        .btn-cancelar {
            background-color: #B7C9C7;
            color: #1e5552;
        }

        .btn-cancelar:hover {
            background-color: #9aacab;
            color: #143c3a;
        }

        .btn-distribuir {
            background-color: #002B80;
            color: white;
        }

        .btn-distribuir:hover {
            background-color: #00215c;
            color: white;
        }

    </style>
</head>

<body>

{{>header}}

<main class="container mb-5 mt-3">
    <h1 class="mt-5" style="font-weight:700; font-family: 'Poppins', sans serif; color: #1e5552">Distribuir viandas</h1>
    <p class="mb-5" style="font-family: 'Poppins', sans serif;">Elegí las heladeras origen y destino para comenzar a distribuir</p>

    <div class="heladeras-container mb-5">
        <form id="distribuirForm" action="/donarDistribuirViandas" method="post">
            <div class="">
                <div class="row mb-3">
                    <!-- Heladera origen -->
                    <div class="col-md-6">
                        <h4 style="font-weight:700; font-family: 'Poppins', sans serif;">Heladera origen</h4>
                        <div class="heladera-card">
                            <strong>{{heladera1}}</strong>
                            <p>{{direccion1}}</p>
                            <p><strong>Estado:</strong> {{estado1}}</p>
                            <p><strong>Necesita traslado de:</strong>{{disponibilidad1}}</p>

                            <div class="green-box">
                                <a href="/mapaHeladerasDistribucionOrigen?heladera1={{heladera1}}&direccion1={{direccion1}}&estado1={{estado1}}&disponibilidad1={{disponibilidad1}}&heladera2={{heladera2}}&direccion2={{direccion2}}&estado2={{estado2}}&disponibilidad2={{disponibilidad2}}" style="text-decoration: none; color: inherit;">Elegir otra Heladera</a>
                            </div>
                        </div>
                    </div>
                    <!-- Heladera destino -->
                    <div class="col-md-6">
                        <h4 style="font-weight:700; font-family: 'Poppins', sans serif;">Heladera destino</h4>
                        <!-- Input eliminado -->
                        <!--<input type="text" class="form-control" id="heladera-destino" placeholder="Heladera Medrano UTN">-->
                        <div class="heladera-card">
                            <strong>{{heladera2}}</strong>
                            <p>{{direccion2}}</p>
                            <p><strong>Estado:</strong> {{estado2}}</p>
                            <p><strong>Disponibilidad:</strong>{{disponibilidad2}}</p>
                            <div class="green-box">
                                <a href="/mapaHeladerasDistribucionDestino?heladera1={{heladera1}}&direccion1={{direccion1}}&estado1={{estado1}}&disponibilidad1={{disponibilidad1}}&heladera2={{heladera2}}&direccion2={{direccion2}}&estado2={{estado2}}&disponibilidad2={{disponibilidad2}}" style="text-decoration: none; color: inherit;">Elegir otra Heladera</a>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="row mb-3">
                    <!-- Motivo -->
                    <div class="col-md-6">
                        <p style="font-weight:700; ">Motivo</p>
                        <select name="motivoDistribucion" id="motivo" class="form-control">
                            <option value="1">DESPERFECTO</option>
                            <option value="2">FALTA DE VIANDAS</option>
                        </select>
                    </div>
                    <!-- Cantidad de viandas -->
                    <div class="col-md-6">
                        <p style="font-weight:700; ">Cantidad de viandas</p>
                        <select name="cantidadViandas" id="cantidad" class="form-control">
                            <option value="1">1</option>
                            <option value="2">2</option>
                            <option value="3">3</option>
                        </select>
                    </div>
                </div>
            </div>
            <div class="">
                <div class="btn-group d-flex justify-content-center">
                    <button type="button" class="btn-cancelar" onclick="window.history.back()">Cancelar</button>
                    <button type="submit" class="btn-distribuir" onclick="submitForm()">Distribuir</button>
                </div>
            </div>
        </form>
        {{#errorDistribuir}}
            <div class="error-message">{{errorDistribuir}}</div>
        {{/errorDistribuir}}
        <script>
        function submitForm() {
        var form = document.getElementById('distribuirForm');
        if (form.checkValidity()) {
            form.submit();
            window.location.href = '/graciasPorDonar';
        } else {
            form.reportValidity();
        }
    }
        </script>
    </div>
</main>

{{>footer}}

<!-- Scripts de Bootstrap y jQuery -->
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>